[package]
name = "hoist"
version = "0.2.3"
edition = "2024"

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
nix = { version = "0.29", features = ["signal", "process", "resource", "user"] }
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"

